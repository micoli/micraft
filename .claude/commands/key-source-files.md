# key-source-files

Key source files by layer.

> **Use `/codebase-memory` (graph MCP) first** to locate symbols precisely — `search_graph(name_pattern="...")`, `trace_path(...)`, `get_code_snippet(...)`. This table is orientation; the graph is authoritative for call chains and current locations.

