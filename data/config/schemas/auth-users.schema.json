{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AuthUsersConfig",
  "description": "Local auth user list loaded from data/config/auth/users.yaml.",
  "type": "object",
  "additionalProperties": false,
  "required": ["users"],
  "properties": {
    "users": {
      "type": "array",
      "description": "List of registered local users.",
      "items": { "$ref": "#/$defs/userEntry" },
      "default": []
    }
  },
  "$defs": {
    "userEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["email", "passwordHash"],
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "User email address — used as login identifier and playerId."
        },
        "passwordHash": {
          "type": "string",
          "pattern": "^\\$2[aby]\\$[0-9]{2}\\$.{53}$",
          "description": "BCrypt hash of the password (cost factor 12, generated by /adduser)."
        },
        "displayName": {
          "type": "string",
          "minLength": 1,
          "description": "Display name shown in-game. Defaults to email if omitted."
        }
      }
    }
  }
}
