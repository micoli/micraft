# Pinned. Bumped by Dependabot (pip ecosystem, /docs).
# NOTE: mkdocs-gen-files / mkdocs-literate-nav >= 0.6.1 / 0.6.3 pull in the
# `properdocs` fork as a hard dependency — keep these two below that line so the
# toolchain stays on upstream `mkdocs` only.
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-gen-files==0.6.0
mkdocs-literate-nav==0.6.3
pymdown-extensions==11.0.2
