world:
  worldMinY: 0
  worldMaxY: 256
  chunkSize: 16
  viewRadius: 3
  forwardViewRadius: 7
  waterLevel: 65
  impostorSkirtDepth: 12
  chunkKeepMargin: 2            # extra chunk radius kept in memory beyond the streaming radius
  chunkUnloadGraceSeconds: 120  # keep an out-of-range chunk this long before unloading (reconnect)
player:
  heightStanding: 1.8
  heightSneaking: 1.5
  heightCrawling: 0.6
  width: 0.6
  eyeOffsetStanding: 1.62
  eyeOffsetSneaking: 1.27
  eyeOffsetCrawling: 0.4
  speedStanding: 4.5
  speedSneaking: 1.3
  speedCrawling: 1.0
auth:
  provider: none
  local:
    usersFile: data/config/auth/users.yaml
    groupsFile: data/config/auth/groups.yaml
chunks:
  transport: websocket
  httpWorkers: 4
network:
  messageEncoder: protobuf
game:
  tickMs: 50
  gravity: -20.0
  jumpSpeed: 8.5
  flyVerticalSpeed: 8.0
  saveIntervalSeconds: 30
  spawnX: 8.0
  spawnY: 200.0
  spawnZ: 8.0
  ticksPerDay: 72000
  timeBroadcastTicks: 20
  maxInteractionDistance: 14.0
  reconcileToleranceXz: 0.5
  reconcileToleranceY: 0.99
  blockBreakBufferSize: 1000
factions:
  enabled: false
  friendlyFire: false
  changeCooldownSeconds: 0
  # Auto faction spawns are spread on a ring of this radius (blocks) around the origin,
  # each pushed outward to a distinct Voronoi cell of zone level < 5.
  spawnRingRadius: 384
  list: []
  # Example (1 to 5 factions), set enabled: true to activate:
  # list:
  #   - id: red
  #     name: "Ordre Écarlate"
  #     color: "#c0392b"
  #     description: "Les défenseurs du Nord."
  #     # optional explicit spawn (must sit in a zone level < 5); omit for auto ring placement
  #     # spawnX: -300
  #     # spawnZ: 120
  #   - id: blue
  #     name: "Pacte d'Azur"
  #     color: "#2980b9"
  #     description: "Les marchands du littoral."
